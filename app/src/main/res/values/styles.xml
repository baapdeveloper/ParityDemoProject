<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="tab_text_appearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MyCustomTabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="tabMaxWidth">264dp</item>
        <item name="tabIndicatorColor">@android:color/holo_orange_light</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabBackground">@android:color/holo_blue_light</item>
        <item name="tabTextAppearance">@style/tab_text_appearance</item>
        <item name="android:colorBackground">@android:color/holo_blue_light</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
    </style>

</resources>
